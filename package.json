{
  "name": "arena-gateway",
  "version": "1.0.0",
  "scripts": {
    "install:browsers": "npx puppeteer browsers install",
    "start": "npm run install:browsers && node server.js"
  },
  "dependencies": {
    "express": "^5.1.0",
    "puppeteer": "^24.19.0"
  }
}